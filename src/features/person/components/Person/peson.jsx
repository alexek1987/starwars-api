// import Starship from '../Starship/Starship'

// interface PersonModel {
//   name: string
//   gender: string
//   height: string
// }

// interface Props {
//   person: PersonModel
//   starships: string[]
// }

// interface IShip {
//   ships: string[]
//   name: string
// }

// const Person: React.FC<Props> = ({ person, starships }) => {
//   const [ships, setShips] = useState<IShip[]>([])

// const fetchStarship = async ({ url }) => {
//   const res = await fetch(url)
//   return res.json()
// }

// Loop over starships and set ships array

// useEffect(() => {
//   if (starships) {
//     for (let i = 0; i < starships.length; i += 1) {
//       ;(async () => {
//         const res = await fetchStarship(starships[i])
//         setShips(prev => [...prev, res])
//       })()
//     }
//   }
// }, [starships])

{
  /* {ships.length ? <h1 className='text-xs'>Starships</h1> : null}
      {Children.toArray(ships?.map(ship => <Starship name={ship.name} />))} */
}
{
  /* </div>
  )
}

export default Person */
}
